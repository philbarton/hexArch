A sample project to demonstrate the Ports and Adapters (Hexagonal) architecture.

Broken down into:
- common adapter code
- a command line adapter
- the application logic

````
mvn clean package
java -jar adapter_cli/target/adapter_cli-1.0-SNAPSHOT-jar-with-dependencies.jar
````